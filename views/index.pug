extends layout

block stylesheets

block content
  .main-container
    .draw-container
    .chat-container
  footer
    button.participants
      span#numUsers 0
      | &nbsp;🤖
    button#chatToggle.disabled(disabled)

block scripts
  script(src='https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.2/socket.io.js')
  script(src='/dist/bundle.js')
